$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: inference_pipeline
display_name: Run inference pipeline
version: 1
type: command
inputs:
  config:
    type: string
    default: config.yaml
outputs:
  ready:
    type: uri_file
code: ../../
environment: azureml:airquality_env@latest
command: >-
  python src/components/comp_inference_pipeline.py ${{inputs.config}}
